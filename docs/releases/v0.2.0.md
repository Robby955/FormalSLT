# FormalSLT v0.2.0 candidate record

**Status: not released.** This document describes the intended v0.2.0 artifact
and its publication gate. Its presence does not assert that a `v0.2.0` tag,
GitHub Release, archived artifact, or DOI exists.

## Candidate identity

| Field | Current candidate evidence | Publication requirement |
|---|---|---|
| Historical theorem-integration checkpoint | ancestor commit `e029eb22348e74d60f731419b9158ce9ae8e807c`, tree `219b9bd7d2eeedd2c08397e57ecee6aa45bc2fc8` | This identifies the integrated theorem checkpoint, not the current candidate or eventual release commit. |
| Historical public review | [draft PR #99](https://github.com/Robby955/FormalSLT/pull/99) was opened at the theorem-integration checkpoint above; hosted core, downstream, and documentation checks passed at that exact historical head on 2026-08-20 | Do not transfer those checks to a later head. Review the release-artifact diff and rerun hosted checks at its exact commit. |
| Historical controlled-queue code-freeze checkpoint | exact commit `5eae99f5f217edc7b44bd81dda6fde2a946effda`, tree `255c20fe5297c1adcc0a4ee3b9e3a308b311f48a` | This checkpoint contains the frozen protocol and tooling but predates the fail-closed OSF final-GUID cross-binding correction. A later reviewed code-freeze checkpoint is required before registration. Neither checkpoint contains a fresh prospective artifact or result. |
| Version tag | Not asserted by this candidate record | An exact `v0.2.0` tag exists on the reviewed release commit. |
| GitHub Release | Not asserted by this candidate record | A GitHub Release targets `v0.2.0` and contains these notes and boundaries. |
| DOI | Not reserved, minted, or recorded here | Reserve the DOI in an unpublished deposit before the final metadata commit if the tagged `CITATION.cff` must contain it; publish the deposit only after the tag receipts pass. |
| Documentation | Candidate sources only | Deployed API and concept-search pages link to the tagged source rather than a moving branch. |

The exact release SHA cannot be committed into the same source tree before the
release commit exists. At publication, record the output of
`git rev-parse v0.2.0^{}` and `git rev-parse v0.2.0^{}^{tree}` in the GitHub
Release, together with links to the hosted verification runs.

### Superseded candidate mechanism check

On 2026-08-20, `scripts/check_tag_install.sh` resolved a temporary local
`v0.2.0-test` tag to commit
`920af752e044263480c26a49106d606b5b240972`, verified that Lake installed the
same commit, and built all four downstream topic imports in 3,958 jobs. That
exercise did not test mutation between separate remote lookups and therefore
does not validate the resolver-bound v2 receipt workflow. The repaired workflow
has local unit, shell, and static checks only until it runs from a real protected
tag. Neither exercise is evidence that the public `v0.2.0` tag, matched hosted
receipts, GitHub Release, or DOI exists.

## Intended release scope

- The four topic imports and 19 declarations in the
  [candidate API policy](../api-stability.md).
- All-sample-size IID empirical-Bernstein PAC-Bayes, adaptive trajectory
  inference, and stationary Poisson and unknown-kernel certification.
- The opt-in 20-state random-refresh load
  [application](../random-refresh-load-application.md).
- The opt-in 24-state controlled-queue
  [application](../controlled-queue-application-design.md), including its
  invariant-risk atom, structured OPE events, aligned known-kernel receipt,
  frozen prospective protocol, and code-freeze tooling.
- Exact v0.1 example replay, downstream installation checks, declaration and
  axiom snapshots, theorem discovery, and literature/provenance documentation.

## Known boundaries

- The IID empirical-Bernstein endpoint is offline, not an optional-stopping
  theorem.
- Trajectory selection ranges over predictors and tilt atoms declared before
  scoring; it does not authorize post-outcome construction of new candidates.
- The named deterministic application path is not proved to belong to the
  theorem-produced good event, so that application step remains conditional.
- Normalized empirical-kernel radii require positive limiting row frequencies;
  candidate-kernel budget convergence additionally requires empirical
  discrepancy convergence.
- Application declarations are not part of the 19-name compatibility promise.
- The prospective oracle true-kernel and fixed-range rows are arithmetic-only
  `PLANNED_NOT_CHECKED` comparisons. Their `1/20` entries are planned
  allocations, not checked outer-mass bounds; neither row has a checked event
  theorem or is a confidence certificate.
- No fresh prospective controlled-queue trace, receipt, or numerical result is
  part of this candidate record. A local protocol, retrospective fixture, or
  compiler smoke is not prospective evidence.
- Full continuous Dudley, all-real predictable tilt optimization, and
  continuous-state stationary Poisson certification remain open. See the
  [scope ledger](../assumptions-and-nonclaims.md).

## Publication gate

Before calling v0.2.0 released:

1. Enable immutable releases, protect `v*` tags from update/deletion, and
   require an approving PR review. On 2026-08-20 all three remained external
   governance gates: immutable releases were disabled, rulesets were empty,
   and branch protection required no review.
2. Put the release-artifact commits on the candidate PR, obtain substantive
   approval, merge, and record the exact resulting commit and tree.
3. Reserve a DOI in an unpublished archival deposit, add that reserved DOI and
   version to `CITATION.cff`, review the metadata delta, and pass the complete
   [release audit](../public-release-checklist.md) on the final exact commit.
   Reservation is not publication or a minted, resolving DOI.
4. If the prospective controlled-queue outcome is included, immutably register
   the protocol and code-freeze checkpoint before reading the selected future
   beacon, perform the single authorized generation, and run
   `make verify-controlled-queue-structured-ope-prospective-receipt`. Publish
   the frozen primary and comparisons regardless of outcome. The archived
   version-one binding omits the final registration GUID; independently saved
   registration response supplies that GUID, the archived-file metadata target
   must match it, and the metadata size and SHA-256 must match the exact binding
   bytes.
5. Create protected tag `v0.2.0`. The automatic tag workflow requires its one
   remote resolution to equal the push event's exact commit, checks out that
   commit on Linux and macOS, and compares every subsequent tag lookup and Lake
   installation to the resolver outputs. Manual dispatch instead establishes
   one live resolution for the named existing tag.
6. Require both operating-system jobs plus the aggregate job. The aggregate
   requires matching tag/object/commit/tree receipts and rechecks the current
   remote. Receipts record identity and environment only; hosted job status is
   the build evidence.
7. Create the actual GitHub Release with both receipts and these boundaries,
   publish the archival deposit, and verify its DOI resolves to the same
   version, tag, commit, and tree.
8. Deploy the generated documentation and verify that source links are pinned
   to the released revision.

Only after steps 1--8 pass is this installation declaration valid:

```lean
require «formal-slt» from git
  "https://github.com/Robby955/FormalSLT.git" @ "v0.2.0"
```

Until then, use an exact reviewed commit as described in the
[README](../../README.md), and cite the current unversioned
[`CITATION.cff`](../../CITATION.cff) without claiming a v0.2 DOI.
